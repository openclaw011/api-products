[build]
builder = "nixpacks"
buildCommand = "cd stock-api && pip install -r requirements.txt"

[deploy]
startCommand = "cd stock-api && gunicorn app:app --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
rootDirectory = "stock-api"
